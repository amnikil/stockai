apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: stockai
spec:
  type: ClusterIP
  selector:
    app: api-gateway
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
