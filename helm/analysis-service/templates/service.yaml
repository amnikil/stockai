apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-service
  namespace: stockai
  labels:
    app: {{ .Chart.Name }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Chart.Name }}
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
